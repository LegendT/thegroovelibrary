# Netlify Configuration for The Groove Library
# @see https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Build command - runs Eleventy build
  command = "npm run build"

  # Directory to publish
  publish = "_site"

  # Node version for builds
  [build.environment]
    NODE_VERSION = "20"

# Use Ubuntu 22.04 (Jammy) build image
[build.processing]
  skip_processing = false

# Build hooks for scheduled updates
# After deployment, you can trigger builds with:
# POST https://api.netlify.com/build_hooks/{HOOK_ID}

# [[plugins]]
#   package = "@netlify/plugin-lighthouse"
#
#   # Optional: Configure Lighthouse plugin for performance monitoring
#   [plugins.inputs]
#     output_path = "reports/lighthouse.html"

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    # Security headers
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

    # Content Security Policy
    # Adjust as needed for your specific requirements
    Content-Security-Policy = """
      default-src 'self';
      script-src 'self' 'unsafe-inline' https://webawesome.com https://kit.webawesome.com https://ka-p.webawesome.com https://cdn.jsdelivr.net;
      style-src 'self' 'unsafe-inline' https://webawesome.com https://kit.webawesome.com https://ka-p.webawesome.com;
      img-src 'self' data: https: blob:;
      font-src 'self' data: https://webawesome.com https://kit.webawesome.com https://ka-p.webawesome.com;
      connect-src 'self' https://api.mixcloud.com;
      frame-src https://www.mixcloud.com https://widget.mixcloud.com https://player.mixcloud.com;
      media-src https://www.mixcloud.com https://widget.mixcloud.com https://player.mixcloud.com;
    """

# Cache headers for static assets
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Redirects
# [[redirects]]
#   from = "/old-path"
#   to = "/new-path"
#   status = 301

# Custom 404 page (optional - create this file if needed)
# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404
